export type Product = {
    name: string,
    price: number,
    discount: number
}

export type Food = {
    nombre: string,
    esVegetariano: boolean
}

export type Persona = {
    nombre: string,
    age: number,
}